--- connection_adapters/mysql_adapter.rb.orig	2012-06-14 15:08:00.711000135 -0400
+++ connection_adapters/mysql_adapter.rb	2012-06-14 15:07:32.382998867 -0400
@@ -403,11 +403,13 @@
       end
 
       def tables(name = nil, database = nil) #:nodoc:
+	tables = []
+
         sql = "SHOW TABLES "
         sql << "IN #{quote_table_name(database)} " if database
 
         result = execute(sql, 'SCHEMA')
-        tables = result.collect { |field| field[0] }
+        result.each { |field| tables << field[0] }
         result.free
         tables
       end
